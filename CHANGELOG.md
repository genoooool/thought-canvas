# Changelog

## 1.2.5 — 2026-07-24

- 在设置通用页新增简体中文、English、日本語界面切换，支持即时预览和本机持久化。
- 界面翻译与项目/用户/模型内容隔离；用户内容即使恰好等于 UI 词条也不会被翻译。
- 新生成请求根据界面语言附带简体中文、英文或日文回答语言指令；日期与数字格式跟随所选语言。
- 新增 `i18n.js` 词典、动态模式翻译、DOM 增量本地化和纯函数测试。
- 定位截图乱码为 UTF-8 字节被 Windows-1252/CP1252 误解码的典型特征。
- 新增 `text-encoding.js` 保守修复器，覆盖历史项目、恢复版本、设置、导入、请求/响应和供应商输出。
- 历史项目自动修复前保留 `encoding-before-*` 备份，并持久化迁移版本、时间及累计修复数。
- 静态 HTML/JavaScript/JSON/NDJSON UTF-8 响应头加入集成测试；复杂乱码、正常中日英文本与模型 ID 加入回归。

## 1.2.4 — 2026-07-24

- 修复长文本和复杂 Markdown 选区在拆解/提炼时偶发丢失或误报的问题；无法唯一映射时保留可见文字并显式标记范围未知。
- 拆解内容模块获得递归拆解和提炼操作，不再依赖先追问生成新节点。
- 顶部项目、最终目标、保存状态改为独立模块；搜索框对齐项目模块。
- 重做新建分支弹窗和通用弹窗视觉层级；移除鼠标编辑时的装饰性双层焦点框。
- 浏览器原生确认框替换为居中的应用内确认弹窗。
- 新增可点击或拖拽创建的标注/想法/问题/风险/行动模块，以及固定 3/3 密集虚线关系。
- 新增分支折叠/展开；搜索命中折叠后代时自动展开路径。
- 标注与折叠状态进入保存、恢复、Thought Canvas JSON、JSON Canvas 和 Markdown 导出。
- 新增 `selection-utils.js` 和纯函数回归测试，扩展 Chromium E2E 覆盖标注拖拽、折叠、弹窗及布局几何。

## 1.2.3 — 2026-07-24

- Codex App Server 现代路径改用 `permissionProfile/list` 与 `permissions: ":read-only"`。
- 移除已废弃的 `sandboxPolicy.readOnly.access`，并保留严格的旧版只读兼容与策略拒绝 fail-closed。
